import { Console } from '@woowacourse/mission-utils';

class OutputView {
  static print(input) {
    Console.print(input);
  }
  //   static printCalendar(calendar,workerCalendar){
  //     calendar.forEach()
  //   }
}

export default OutputView;
